fastapi==0.115.0
uvicorn[standard]==0.32.0
python-dotenv==1.0.0
python-multipart==0.0.12

# Keep http stack stable and compatible with Supabase client.
httpx>=0.26.0,<0.28.0

# Use a Pydantic version with prebuilt wheels on supported Pythons (Railway runtime is 3.11).
pydantic>=2.10.6,<3.0.0

PyPDF2==3.0.1
python-docx>=1.1.0,<2.0.0

# Supabase bundles and pins its own sub-dependencies; do not pin them separately here.
supabase==2.6.0

python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
email-validator==2.1.0
